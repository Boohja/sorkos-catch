export function initCaptureEditing(){
  const root=document.querySelector('[data-capture-detail]');if(!root)return;
  const csrf=root.dataset.csrf||'',captureId=root.dataset.captureId,status=root.querySelector('[data-edit-status]');
  const fields=Array.from(root.querySelectorAll('[data-capture-field][contenteditable="true"]'));
  const announce=(message,error=false)=>{if(!status)return;status.textContent=message;status.classList.toggle('is-error',error)};
  const valueOf=element=>element.innerText.replace(/\r/g,'').trim();
  const setValue=(element,value)=>{element.textContent=value;element.dataset.value=value;element.classList.toggle('is-empty',value==='');if(element.dataset.captureField==='title'){const separator=root.querySelector('[data-title-separator]');if(separator)separator.hidden=value==='';document.title=`${value||'Capture'} | Catch`;}if(element.dataset.captureField==='url'){const link=root.querySelector('[data-open-capture-url]');if(link)link.href=value;const fallback=root.querySelector('[data-url-fallback]');if(fallback&&!root.dataset.hasTitle)fallback.textContent=value||'Add URL';}};
  const save=async element=>{if(element.dataset.saving==='true')return;const before=element.dataset.value||'',value=valueOf(element);if(value===before){setValue(element,before);return;}element.dataset.saving='true';element.setAttribute('aria-busy','true');element.setAttribute('contenteditable','false');announce('Saving…');try{const data=new FormData();data.set('_csrf',csrf);data.set('field',element.dataset.captureField);data.set('value',value);const response=await fetch(`/captures/${encodeURIComponent(captureId)}`,{method:'POST',headers:{Accept:'application/json'},body:data});const json=await response.json().catch(()=>({}));if(!response.ok)throw new Error(json.error||'The change could not be saved.');setValue(element,String(json.value??value));announce('Saved.');}catch(error){setValue(element,before);announce(error.message,true);}finally{delete element.dataset.saving;element.removeAttribute('aria-busy');element.setAttribute('contenteditable','true')}};
  fields.forEach(element=>{setValue(element,valueOf(element));element.addEventListener('focus',()=>{element.dataset.value=valueOf(element)});element.addEventListener('blur',()=>save(element));if(element.dataset.singleLine!==undefined)element.addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();element.blur()}});element.addEventListener('paste',event=>{if(element.dataset.singleLine===undefined)return;event.preventDefault();const text=(event.clipboardData?.getData('text/plain')||'').replace(/\s+/g,' ').trim();document.execCommand('insertText',false,text)});});
}
