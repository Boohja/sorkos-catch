import{initTheme}from'./theme.js';import{onSync,syncOutbox}from'./sync-manager.js';
import{initDevices}from'./devices.js';initTheme();initDevices();if('serviceWorker'in navigator)navigator.serviceWorker.register('/service-worker.js').catch(()=>{});
const toast=document.querySelector('[data-sync-status]'),summary=document.querySelector('[data-sync-summary]');onSync(({state,count=0})=>{const text=state==='offline'?'Offline - lokal gespeichert':state==='syncing'?`${count} werden synchronisiert`:state==='failed'?`${count} warten auf Synchronisierung`:'Alles synchronisiert';if(summary)summary.textContent=text;if(toast&&state!=='synced'){toast.textContent=text;toast.hidden=false;}else if(toast)toast.hidden=true;});
window.addEventListener('online',syncOutbox);document.addEventListener('visibilitychange',()=>document.visibilityState==='visible'&&syncOutbox());syncOutbox();
document.querySelectorAll('[data-copy-button]').forEach(button=>button.addEventListener('click',async event=>{const trigger=event.currentTarget;const row=trigger.closest('[data-copy-row],.pairing-code-row');const value=row?.querySelector('[data-copy-source]')?.textContent?.trim();if(!value)return;await navigator.clipboard.writeText(value);trigger.textContent='Kopiert';}));
const form=document.querySelector('[data-capture-form]');if(form){form.elements.client_capture_id.value=crypto.randomUUID();const input=form.querySelector('textarea');input.addEventListener('input',()=>{form.elements.type.value=/^https?:\/\/\S+$/.test(input.value.trim())?'url':'text';if(form.elements.type.value==='url')form.elements.url?.remove();});}
