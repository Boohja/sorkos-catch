# Catch agent access

Catch data is available through the `catch` CLI.

When the user references `Catch #<id>`, prefer:

```text
catch get <id> --json
```

Prefer the CLI over opening the Catch web UI. Do not display or attempt to retrieve Catch authentication credentials.
